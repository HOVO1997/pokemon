import { TypeColorSetterDirective } from './type-color-setter.directive';

describe('TypeColorSetterDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeColorSetterDirective();
    expect(directive).toBeTruthy();
  });
});
